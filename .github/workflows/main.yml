name: MacRDP

on:
  workflow_dispatch:

jobs:
  build:
    name: MacRDP
    runs-on: macos-latest
    timeout-minutes: 360

    steps:
      - name: Enable VNC
        run: |
          sudo defaults write /Library/Preferences/com.apple.RemoteManagement VNCLegacyPassword -string "P@ssw0rd!"
          sudo launchctl load -w /System/Library/LaunchDaemons/com.apple.screensharing.plist 2>/dev/null || true
          sudo /System/Library/CoreServices/RemoteManagement/ARDAgent.app/Contents/Resources/kickstart \
            -activate -configure -access -on \
            -clientopts -setvnclegacy -vnclegacy yes \
            -clientopts -setvncpw -vncpw P@ssw0rd! \
            -restart -agent -privs -all
          sleep 5
          open -a Finder || true
          open -a Terminal || true
          sleep 3

      - name: Install & Start ngrok
        env:
          NGROK_AUTH_TOKEN: ${{ secrets.NGROK_AUTH_TOKEN }}
        run: |
          brew install ngrok/ngrok/ngrok
          ngrok config add-authtoken $NGROK_AUTH_TOKEN
          ngrok tcp 5900 > /dev/null &
          sleep 8

      - name: VNC Login Details
        run: |
          curl -s http://localhost:4040/api/tunnels | python3 -c "
          import sys, json
          data = json.load(sys.stdin)
          addr = data['tunnels'][0]['public_url'].replace('tcp://', '')
          host, port = addr.split(':')
          print('================================')
          print('VNC HOST    :', host)
          print('VNC PORT    :', port)
          print('VNC USERNAME: runneradmin')
          print('VNC PASSWORD: P@ssw0rd!')
          print('================================')
          "

      - name: Keep Alive
        run: sleep 21600
